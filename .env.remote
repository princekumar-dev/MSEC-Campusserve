# Use this file to test with the deployed Render backend
# Copy this to .env.local or set VITE_USE_REMOTE_API=true in your environment

VITE_USE_REMOTE_API=true
VITE_API_URL=https://academics-2.onrender.com

# Note: When this is set, the frontend will connect to the remote Render API
# instead of localhost:3001
